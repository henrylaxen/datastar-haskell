import ServerSentEventGenerator

-- Merges HTML fragments into the DOM.
send $ mergeFragments "<div id=\"question\">What do you put in a toaster?</div>" def def def

-- Merges signals into the signals.
send $ mergeSignals "{response: '', answer: 'bread'}" def def
